from setuptools import setup

setup(name='flatdir',
      version='0.1',
      description='How to get only files',
      url='',
      author='SDE',
      author_email='seb.delarue@gmail.com',
      license='',
      packages=['flatdir'],
      install_requires=[
            'win32com',
      ],
      zip_safe=False)